
  {% if site_logo %}
    <a class="navbar-brand" href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">
      <img class="mr-2" src="{{ site_logo }}" alt="{{ 'Home'|t }}" />
    </a>
  {% endif %}

